<Application x:Class="ShopServer.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ShopServer"
             xmlns:ViewModel="clr-namespace:ShopServer.ViewModel"
             xmlns:View="clr-namespace:ShopServer.View"
             StartupUri="MainWindow.xaml">
    
    <Application.Resources>
        
        <DataTemplate DataType="{x:Type ViewModel:HomeViewModel}">
            <View:HomeView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type ViewModel:ProduktViewModel}">
            <View:ProduktView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type ViewModel:BestellungenViewModel}">
            <View:BestellungenView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type ViewModel:BestellungViewModel}">
            <View:BestellungView/>
        </DataTemplate>



    </Application.Resources>
    
</Application>
